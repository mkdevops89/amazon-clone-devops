[bastion]
# Static inventory placeholder.
# In a true dynamic environment, we rely entirely on aws_ec2.yaml to resolve host IPs via tags.
# If you wish to bypass the AWS plugin, you can manually enter the Bastion IP here:
# 3.84.12.56
